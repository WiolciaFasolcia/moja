<%= simple_form_for @user do |f| %>
  <%= f.error_notification %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="form-inputs">
    <%= f.input :imie, required: true, autofocus: true, label: 'Imię' %>
    <%= f.input :nazwisko, required: true, label: 'Nazwisko' %>
    <%= f.input :email, required: true, autofocus: true %>
     <%= f.input :admin, autofocus: true %>
    <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @validatable) %>
    <%= f.input :password_confirmation, required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Zapisz", :class => 'btn btn-primary' %>
    <%= link_to 'Cofnij', users_path, :class => 'btn btn-primary' %>
  </div>
<% end %>

